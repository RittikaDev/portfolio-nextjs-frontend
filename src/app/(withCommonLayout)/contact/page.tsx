import Contact from "@/components/Contact";
import React from "react";
import { Metadata } from "next";

export const metadata: Metadata = {
	title: "Rittika Dev | Contact",
};

const ContactPage = () => {
	return <Contact />;
};

export default ContactPage;
